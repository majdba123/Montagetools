# P3/P4 current sparse-interval audit

Continuation from clean `83ef4d27d8013780b7a261c869893b2c1f91c48f`; P1/P2 remain protected. No source, Vision cache, physical lifetime, causal preset, partition contract or QA threshold changed.

## Encoded baseline inspected once

Newest export: `HEXA_INSUFFICIENT_BALANCE_AR_V31_0_25_20260910-051621-39a7b10e.mp4`.
SHA256: `dca5a7ae6b31309632e3d87fa1fab87e11a4eab71115e5e4ba9e567113224d53`.
Read-only FFmpeg sampling at 4 Hz, 320x180, area scaling, RGB24; same thresholds as the existing reference audit. Mean occupied pixels **18.9249%**, mean changed pixels **10.8533%**, near-static frame differences **26.7677%**. These are baseline measurements, not measurements of the new code.

## Limited source-use audit

| Diagnostic interval | Planner ownership | Evidence and classification |
|---|---|---|
| 48-51 s | VCARD_011; SCENE_028, SCENE_029 | Established root is a composite illustration. Foundation person/detail masks exist, but their partition is incomplete and physically parent-dependent. Root fallback is correct; no legitimate independent actor was found omitted. Later support requires the left-side space. |
| 61.5-63.75 s | VCARD_014; SCENE_035, SCENE_036 | The established merchant illustration is a single root. Candidate child masks have incomplete partition and physical foreground contact. Independent reveals cannot be safely granted. Low bottom placement limits enlargement before later upper support. |
| 73.5-78.25 s | VCARD_016/017; SCENE_040/041 | Two source roots already serve the cause/reaction clock; the thin arrow contributes little ink. The following scene uses the complete child-plus-residual partition. Both carriers are present. Splitting or independently animating the reconstruction support would violate its authority. |

These timestamps and identities are diagnostic only. No product rule uses them. More source detail does not imply more independently movable or revealable actors. The audit found no basis for a P1 change.

## Generic changes

- Reserve bounded temporary-framing search before lifetime-wide fits when a later carrier enters the sparse interval. Try the strongest bounded scale first. The envelope can begin with the existing entry while retaining its exact preset timing and opacity; explicit restoration still completes by the incoming carrier's reveal. Full-lifetime collision, safe-frame and material-gain checks remain mandatory.
- Distribute noncausal source reveals and the two attributable focus/rebuild destinations across the available spoken/cohort clock. Long ideas reserve a readable final hold. Short or constrained clocks retain the compact sequence, and existing P2 causal reveals remain exact.
- Regression coverage includes an introduction whose useful framing interval overlaps its entry and a 12-second three-actor sentence with late focus/rebuild, in addition to existing sealed-plan, actual-pixel and encoded-attribution checks.

## Remaining engineering boundary

The bounded finalizer probe on this production plan still commits **zero** P3 residual enlargements. Projected mean card ink remains **17.1542%**. The unresolved severe cards are 003, 005, 006, 007, 008, 009, 010, 013, 014, 016, 017 and 019. Stronger fixed-center candidates clip; static refits can collide with later support. Preserving those rejections is intentional, not a passing P3 result.

P4 retains five accepted source-backed sequences. The last accepted card's actual-source 4 Hz near-static estimate improves from the previous implementation's 0.2941 to 0.2353 (unsequenced baseline 0.6471); the other four accepted sequences retain their prior estimates. This is targeted runtime evidence, not canonical encoded acceptance. Broader distribution remains constrained by shared actor lifetimes, partition authority and source independence.

**P3 ENGINEERING OPEN. P4 ENGINEERING OPEN for the requested broader production progression. P3/P4 ENCODED OPEN.** A green software/certification suite does not close these visual outcomes. Reference grammar remains in `docs/P4_LOCAL_REFERENCE_MOTION_GRAMMAR.md`; originals were not resampled or changed in this continuation. Temporary media and probes remain under ignored `.hexa_tmp_inspect/`.

## Production-plan replay evidence

One fresh full production-plan replay used the newest render's preserved alignment and Vision report. It completed successfully. Event identities and render modes, physical/motion lifetime bounds, entry/exit/action presets and suppression flags matched the encoded baseline exactly. Final motion immutability and composition QA passed. The fresh P3 stage reports 17.1431% projected mean card ink before and after, zero residual commits, and the same twelve unresolved severe cards. P4 retains the five sequences and static estimates reported above. The small difference from the finalizer-only probe's 17.1542% is stage context, not encoded improvement.

## Software verification before shipping

Targeted sparse-framing and two/three-actor pixel/MP4 attribution tests passed, including the new short-entry and long-spoken-idea regressions. All three production interaction certification tests passed. The complete deterministic V31 suite completed with exit code 0 and `V31_0_9_SEMANTIC_PHASE_REPARTITION_COMPILER_TEST_SUITE_PASS`. Shipping/install and exact-head CI evidence are reported in the final handoff; these checks must not be read as encoded visual closure.
